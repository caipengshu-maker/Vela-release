项目：C:\Users\caipe\.openclaw\workspace\Vela
模式：续跑（上轮被 429 打断，不重开全量探索）
施工位：Codex CLI（GPT-5.4 xhigh）

你上轮已完成大量读仓并定位了改动面，但因 429 被中断。现在请按“续跑模式”执行：

## 目标（不变）
继续完成 Vela M3 round 2：
1) 运行面稳定（Electron/preload/VRM）
2) avatar 主舞台权重提升
3) 主界面状态表达去工具味、转自然在场提示
4) `idle/listening/thinking/speaking` + `wide/close` 最小统一表演语言

## 约束（不变）
- 不跑到 M4（关系深化/旧话题续接/轻主动都不做）
- 不造新文档体系
- 不回滚已有主链有效改动

## 执行策略（新增，针对 429）
- 只做最小必要读文件，不重复全量扫描
- 直接进入修改 + 验证
- 如果再次遇到 429：先输出“已完成到哪一步 + 下一步最小续跑入口”，再退出

## 最低交付
- 修改文件清单
- `npm run build` + `npm run smoke` 结果
- 一段简明 closure：
  - 本轮解决了什么
  - 还剩什么留到下一刀

直接续跑，不要重做背景分析。