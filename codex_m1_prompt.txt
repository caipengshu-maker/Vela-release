你现在是 Vela 项目的默认编码施工位。请在一个连续施工上下文内完成 `M1-T1 ~ M1-T5` 的首批最小骨架，不要扩 scope，不要把任务拆成多个会话思路。

项目目录：C:\Users\caipe\.openclaw\workspace\Vela

开工前先阅读并对齐：
- M0_FREEZE.md
- CURRENT-ROUTE.md
- TASKS.md

必须遵守的约束：
1. 一期先做纯聊天 avatar 伴侣，不开放文件/系统/编码助手权限
2. 配置系统学习 OpenClaw：用户级真源只用一个主配置文件（建议 `vela.jsonc` 或等价方案）集中管理 LLM / ASR / TTS / 权限 / 主动机制 / runtime；内部可模块化消费，但不要拆一地真源配置
3. UI 审美明确参考二次元乙游，避免强 AI 工具味 / 后台管理台味 / demo 味
4. 当前没有正式 avatar 资产，允许先用占位 avatar / 极简状态 UI 起步，但仍要保证干净、轻、像产品
5. 不要自行扩 scope 到语音重实现、复杂 avatar 资产、复杂主动机制、工具权限
6. 若产生对 OpenClaw 有通用价值的源码，先在代码结构上保持可抽象性，但不要因此拖慢 Vela 主线
7. 整个项目开发阶段必须全程使用 Git 管理；若当前目录尚未初始化 Git，请以最稳妥方式初始化，并保证后续变更可追踪

本轮目标：

M1-T1 Vela 工程骨架初始化
- 初始化 Electron + Web UI 项目骨架
- 保证桌面应用可以启动
- 建一个干净的聊天视图和 avatar/状态区域占位

M1-T2 Vela Core 最小闭环
- 实现本地 `Vela Core` 最小入口，不做完整 gateway
- 至少包含：记忆读写入口、会话状态入口、上下文组装入口、avatar 状态映射入口、本地持久化入口

M1-T3 记忆文件骨架
- 做一期最小记忆结构：profile / relationship / session summaries（或更克制的等价结构）
- 每轮会话结束可写摘要；下轮能读回最近摘要
- 避免过度设计，不做复杂权重/衰减系统

M1-T4 固定人格种子接入
- 先接一个保守默认人格种子：克制、聪明、温柔偏冷、记性好、不油、不装可爱、会自然提旧事
- 目标是人格稳定，不明显漂

M1-T5 文本聊天闭环
- 打通文本模式下的最小聊天闭环
- 回复能带入最近摘要 + 人格上下文
- 用户应感觉“不像重新开机”

交付要求：
1. 先完成能跑的最小原型，再补必要的结构整理
2. 必须提供清晰运行方式
3. 必须做最小验证（启动、聊天、摘要写入/读回）
4. 在当前工作完成后，给出：
   - 改了哪些文件
   - 如何运行
   - 测试/验证结果
   - 当前还缺什么
   - 最关键的架构判断
5. 如果中途卡住，只报最硬 blocker，不要长篇散文化

优先目标不是做花，而是让 Vela 看起来像一个将来能成为伴侣的产品骨架。