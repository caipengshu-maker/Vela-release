项目根目录：C:\Users\caipe\.openclaw\workspace\Vela

任务：只做 M3 最小 VRM 表现层接入。不要改 M1-M4 路线定义，不回灌 M2，不接 TTS 主链，不做完整 body 动画/手势库/物理调参 UI。稳定一致性优先，别追花活。

先读这些文件：
- C:\Users\caipe\.openclaw\workspace\Vela\docs\M3-MINIMAL-PRESENCE-SPEC.md
- C:\Users\caipe\.openclaw\workspace\Vela\docs\M2-M3-STATE-EMOTION-WIRING.md
- C:\Users\caipe\.openclaw\workspace\Vela\src\App.jsx
- C:\Users\caipe\.openclaw\workspace\Vela\src\core\avatar-state.js
- C:\Users\caipe\.openclaw\workspace\Vela\src\core\interaction-policy.js
- C:\Users\caipe\.openclaw\workspace\Vela\src\core\interaction-contract.js
- C:\Users\caipe\.openclaw\workspace\Vela\vela.jsonc

资产：
- 源文件：D:\BaiduNetdiskDownload\EKU\VRM\Eku_VRM_v1_0_0.vrm（已验证可真实加载）
- 运行时资产位：D:/Vela/assets/avatars/eku/（施工时复制建立；不要长期绑百度网盘下载目录）

必须完成：
1. 安装并接入 `three` 与 `@pixiv/three-vrm`
2. 在 Vela 内真实显示 EKU VRM，不再只是 CSS silhouette
3. 保持现有裁决顺序：State Machine → Emotion Plan → Camera Plan → Expression/Action
4. 实现最小状态差异：`idle / listening / thinking / speaking`
5. 实现最小镜头两态：`wide / close`
6. 实现最小 emotion → expression/motion 映射，至少满足：
   - calm → neutral / still
   - happy → happy / tiny-nod
   - affectionate → relaxed / soft-lean
   - playful → happy / tiny-head-tilt
   - concerned → relaxed / soft-lean
   - sad → sad / head-down-light
   - angry → angry / still
   - whisper → relaxed / soft-lean + 优先 close
7. 实现最小活感：自动眨眼 + 极轻微 idle/listening/thinking/speaking 差异
8. 不允许错配：
   - sad/angry 绝不配 smile/head-tilt 卖萌
   - thinking 不要笑脸乱跳
   - provider 原始流和 TTS 不得直接驱动动作/镜头主逻辑

实现建议：
- 新建类似 `src/core/vrm-avatar-controller.js` 的控制层文件，负责 load/update，不要把 three 逻辑塞满 App.jsx
- App.jsx 只做 state → avatar panel props 透传
- 可以保留现有容器结构，但 avatar 区内部替换成真实 VRM canvas
- 若需要补配置，在 `vela.jsonc` 追加最小 avatar asset path 字段，保持配置风格一致

DoD：
- Vela 内真实显示 EKU VRM
- wide/close 可切，切换别生硬抽风
- idle/listening/thinking/speaking 有肉眼可区分的最小差异
- emotion → expression/motion 不错配
- 至少提供一组可复跑验证步骤

你完成后必须回报：
1. 实际修改文件清单（含新增文件）
2. `D:/Vela/assets/avatars/eku/Eku_VRM_v1_0_0.vrm` 是否已就位
3. `three`、`@pixiv/three-vrm` 是否已进入依赖
4. 你实际跑了哪些验证，结果如何
5. 还剩哪些已知限制
6. 如果要回滚，应回到哪个 commit

直接开工。先求稳定，不求花。